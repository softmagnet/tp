package seedu.times.ui.timetabletab;

/**
 * A UI for the header label for the timetable.
 */
public class TimetableHeaderLabel extends TimetableHeader {

    /**
     * Creates a new TimetableHeaderLabel with a string as its input and its width.
     *
     * @param headerStr String to be displayed on the header.
     * @param width Width of the header.
     */
    public TimetableHeaderLabel(String headerStr, int width) {
        super(headerStr, width);
    }

}
